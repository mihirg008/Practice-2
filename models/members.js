const mongoose = require('mongoose');
mongose.Promise = global.Promise;
cont slug = require('slugs');

cont memberSchema = new mongoose.Schema({

});

module.exports = mongoose.model('Members', memberSchema)
